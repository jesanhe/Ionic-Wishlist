import { FiltroComppletadoPipe } from './filtro-comppletado.pipe';

describe('FiltroComppletadoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroComppletadoPipe();
    expect(pipe).toBeTruthy();
  });
});
